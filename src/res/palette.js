import colors from "./colors";

const palette = {
  base: {
    colors: {
      primary: colors.primary,
      secondary: colors.secondary,
    },
  },
  rightHeaderButton: {
    marginRight: 8,
    borderRadius: 60,
  },
};

export default palette;
